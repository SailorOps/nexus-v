{
  "name": "[B",
  "displayName": "MyAwesomeExtension",
  "publisher": "ls -R",
  "description": "MIT",
  "version": "0.0.1",
  "license": "MIT",
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:[B.helloWorld"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "[B.helloWorld",
        "title": "MyAwesomeExtension"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run build",
    "build": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "test": "echo \"No tests yet\""
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/vscode": "^1.90.0"
  }
}

